#include<iostream>
#include<cstring>
using namespace std;

char str[51]={};


int main(void)
{
	int T=0, opencnt=0, closecnt=0;
	cin >> T;
	
	while(T--)
	{
		cin >> str;
		int len = strlen(str);
		if(len%2) {cout << 'NO' << '\n'; continue;}
		
		while(len--)
		{
			
		} 
		
		
		
		if(){'끝에 도달하기 전까지는 (갯수가 많아야 함, (와 ) 갯수 일치'}
	
		cout << 'YES' << '\n';
		
		
		
		
	}
	
	
	return 0;
}
